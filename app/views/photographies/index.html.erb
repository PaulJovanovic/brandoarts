<div class="ptl pbl">
	<% if @photographies.count > 0 %>
		<% controller_singular = "photo" %>
		<% index = 0 %>
		<% @photographies.each do |photography| %>
			<% if index % 3 == 0 %>
				<div class="container">
					<div class="padding pvn">
						<div class="section">
							<div class="row">
			<% end %>
								<%= "<div class='project' data-controller='#{controller_singular}' data-id='#{photography.id}'><div class='inner'>".html_safe %>
									<%= image_tag photography.photos.first.image.url(:small) %>
									</div>
								</div>
			<% if index % 3 == 2  || index == @photographies.length - 1 %>
							</div>
						</div>
					</div>
				</div>
			<% end %>
			<% index += 1 %>
		<% end %>
	<% else %>
		<div class="container">
			<div class="padding">
				<div class="section">
					<h1>No projects yet!</h1>
				</div>
			</div>
		</div>
	<% end %>
</div>