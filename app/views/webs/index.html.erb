<div class="ptl pbl">
	<% if @webs.count > 0 %>
		<% index = 0 %>
		<% @webs.each do |web| %>
			<% if index % 3 == 0 %>
				<div class="container">
					<div class="padding pvn">
						<div class="section">
							<div class="row">
			<% end %>
								<%= "<div class='project' data-title='true' data-controller='#{controller_name[0..-2]}' data-id='#{web.id}'><div class='inner' style='background-color:#{web.photos.first.background_color}'>".html_safe %>
									<%= image_tag web.photos.first.image.url(:small) %>
									</div>
								</div>
			<% if index % 3 == 2  || index == @webs.length - 1 %>
							</div>
						</div>
					</div>
				</div>
			<% end %>
			<% index += 1 %>
		<% end %>
	<% else %>
		<div class="container">
			<div class="padding">
				<div class="section">
					<h1>No projects yet!</h1>
				</div>
			</div>
		</div>
	<% end %>
</div>